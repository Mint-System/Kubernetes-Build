apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data-taskfile-build
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  {{- if .Values.storageClassName }}
  storageClassName: {{ .Values.storageClassName }}
  {{- end }}
