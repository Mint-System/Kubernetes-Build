apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-configmap
  labels:
    app: odoo
data:
  ADDONS_GIT_REPOS: {{ join "," .Values.addonsGitRepos | quote }}
  ODOO_DATABASE: {{ .Values.database }}
  ODOO_INIT_LANG: {{ .Values.init.lang | quote }}
  ODOO_INIT_LOGIN: {{ .Values.init.login | quote }}
  ODOO_INIT_PASSWORD: {{ .Values.init.password | quote }}
  DB_NAME: {{ .Values.database }}
